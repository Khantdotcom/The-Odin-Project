import { Home } from "./modules";
const content_display = document.getElementById("content");
function InitialReload() {
    content_display.removeChild
    const content = document.createElement("div");
    content.innerHTML += "Set up an HTML skeleton inside of src/template.html. Inside the body, add a <header> element that contains a <nav> with buttons (not links!) for different “tabs” (for example buttons for “Home”, “Menu” or “About” etc). Below the <header>, add a single <div id";
};
const dispaly = new Home(content_display);

